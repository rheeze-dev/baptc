@model src.Models.Organization
@{
    ViewData["Title"] = "Settings";
    Layout = "_LayoutAdminlteSettings";
}

<!-- Content Header (Page header) -->
<!-- Main content -->
<link rel="stylesheet" href="~/lib/DateTimePicker/bootstrap-datetimepicker.min.css" />
<section class="content">
    <div class="row">
        <div class="col-lg-12">
            <div class="box box-primary">
                <div class="box-body">
                    <div class="row">
                        <div class="col-lg-12">
                                @*<div class="col-md-6">
                                    <a class="btn btn-success" style="margin-bottom:10px" onclick="ShowPopup('@Url.Action("AddEditTicketingPrice","Settings", new { org = Model.organizationId })')"><i class="fa fa-plus"></i> New ticketing price</a>
                                </div>*@
                            <input type="hidden" asp-for="@Model.organizationId" />
                            <table id="grid" name="grid" class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th class="col-md-1">Traders with transaction</th>
                                        <th class="col-md-1">Traders without transaction</th>
                                        <th class="col-md-1">Farmers single tire</th>
                                        <th class="col-md-1">Farmers double tire</th>
                                        <th class="col-md-1">Short trip pick-up</th>
                                        <th class="col-md-1">Short trip delivery</th>
                                        <th class="col-md-1">Pay parking daytime</th>
                                        <th class="col-md-1">Pay parking overnight</th>
                                        <th class="col-md-1">Gate pass</th>
                                        <th class="col-md-1">Modifier</th>
                                        <th class="col-md-1">Action</th>
                                    </tr>
                                </thead>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</section>
<!-- /.content -->
<!--modal placeholder-->
<div class="modal modal-primary fade" id="modalDefault">
    <div class="modal-dialog">
        <div class="modal-content">

        </div>
    </div>
</div>

@section Scripts{
    @await Html.PartialAsync("_ValidationScriptsPartial")
    <script src="~/js/views/roles/ticketingPrice.js"></script>
    <script src="~/lib/DateTimePicker/bootstrap-datetimepicker.min.js"></script>
}

